[metadata]
license_file = LICENSE

[options.extras_require]
dev =
    pytest
    black
    flake8

[flake8]
max-line-length = 100
exclude = .git,__pycache__,build,dist,.venv

[tool:pytest]
addopts = -ra -q
